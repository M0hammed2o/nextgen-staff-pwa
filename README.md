# NextGen AI — Staff PWA

Mobile-first Progressive Web App for kitchen and front-of-house staff.

## Quick Start
```bash
npm install
npm run dev -- --port 8083
```

Connects to backend at `http://localhost:8000`.
